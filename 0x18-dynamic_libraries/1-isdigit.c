#include "main.h"

/**
 * _isdigit - Checks if the character is a digit
 * @x: Is the number to check
 * Return: 1 if character is a digit or 0 if any else
 */

int _isdigit(int x)

{

if (x >= 48 && x <= 57)
{
return (1);
}
return (0);

}
